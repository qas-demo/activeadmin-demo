dependencies:
  override:
    - sudo apt-key adv --keyserver hkp://pool.sks-keyservers.net --recv-keys 023EDB0B
    - sudo apt-add-repository 'deb https://dl.bintray.com/gauge/gauge-deb stable main'
    - sudo apt-get update
    - sudo apt-get install gauge=0.7.0
    - gauge_setup
    - gauge --install js
    - gauge --install xml-report

test:
  override: 
    - cd tests; npm run test
  post:
    - cp -R tests/reports $CIRCLE_ARTIFACTS
    - cp -R tests/reports/xml-report/results.xml $CIRCLE_TEST_REPORTS

notify:
  webhooks:
    - url: https://pulse.qas-labs.ngrok.io/api/v1/webhooks/LHCQ9yMAALtLaMHJq/4saT2uRbEvcNqAaqX/3543735b-e928-444e-95ee-04163caefd53
